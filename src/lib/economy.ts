// Pure shared economy constants. Keep this module browser-safe so server logic
// and client-facing product copy cannot drift.
export const ONESHOT_GREEN_REWARD = 50;
export const SLOT_STAKE_RATIO = 0.05;
